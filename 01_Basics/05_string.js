const name = "sumit";
const repoCount = 50;

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);

const getName = new String('sumit');

console.log(getName[0]);
console.log(getName.toUpperCase());